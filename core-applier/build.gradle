apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    testCompile "junit:junit:${global_junitVersion}"
    compileOnly project(':api-applier')
    compileOnly project(':archive-patch-applier')
    compileOnly project(':archive-patch-shared')

//    compile "com.google.archivepatcher:archive-patch-shared:1.0.0"
//    compile "com.google.archivepatcher:archive-patch-applier:1.0.0"
//    compile "io.github.lizhangqu:corepatch-api-applier:1.0.0"
}

apply from: project.rootProject.file('gradle/artifact_release.gradle')