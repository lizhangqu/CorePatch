apply plugin: 'java'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    testImplementation "junit:junit:${global_junitVersion}"
//    compileOnly project(':api-generator')
//    compileOnly project(':archive-patch-generator')
//    compileOnly project(':archive-patch-shared')

    implementation "com.google.archivepatcher:archive-patch-shared:1.0.0"
    implementation "com.google.archivepatcher:archive-patch-generator:1.0.0"
    implementation "io.github.lizhangqu:corepatch-api-generator:1.0.0"
}

apply from: project.rootProject.file('gradle/artifact_release.gradle')